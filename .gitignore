# Generated conversion outputs
output_project/

# Per-run regression test reports
tests/reports/

# Cached pipeline outputs for tests (keyed by source hash)
tests/_work/

# Local/private source images + pipeline outputs
slides_private/
input/
output/
tmp/

# Python environments and caches
.venv/
venv/
__pycache__/
*.py[cod]

# Local tool/editor files
.DS_Store
.claude/

# Runtime OCR/review artifacts when written outside output_project/
*.ocr.raw.json
*.ocr.corrected.json

# Local model weights and exported model files
*.pt
*.pth
*.onnx
# Exception: ship the font classifier so the pipeline works after clone (~20 MB)
!models/font_classifier.onnx

# Rendered QA intermediates
*.pdf

# Web layer runtime state (uploads, outputs, SQLite, logs)
web/data/
web/.env
web/backend/.venv/
web/frontend/node_modules/
web/frontend/dist/

latest_log.txt
subprocess_test.log